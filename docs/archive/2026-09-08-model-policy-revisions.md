# 2026-09-08 — 修复已初始化组织不能切换模型

真实恢复计划533febc8在调用模型前停止：固定策略ID/version对应的旧GPT-5.5记录与新模型内容
冲突。旧记录保护正确，缺少的是生产策略版本机制。未启动需求Coder、未产出候选或QA/Review。

## 本阶段产出

- 生产策略根据完整策略内容生成确定版本，Agent身份和默认策略ID不变。
- 普通/恢复交付显式保存不可变策略版本，旧文件和分配记录保持原样。
- Runtime按ModelSelection中的ID和version精确读取，无隐式latest回退。
- 同版本不同内容、损坏记录和symlink拒绝；并发发布使用独占创建后核验胜出记录。
- 增加同组织预存旧策略后更换模型的真实Git/MySQL离线交付回归，以及版本读取和旧记录保护测试。

## 验证

- 关键生产/恢复回归：74 passed，287.58s。
- 全量：892 passed，383.64s；使用专用测试MySQL，不是自迭代业务库。
- Ruff check/format、strict mypy（286files）、offline lock/build、git diff --check通过。
- 没有为测试调用真实模型，没有更改目录默认值需求代码。

## 边界与后续

模型可用性先前已用独立只读探针验证GPT-5.6 Sol；这些离线测试不等于真实需求交付。
平台修复同步后须重新prepare/propose并取得精确批准，才能开始新交付。
旧终态、批准、失败记录不得复活或覆盖；无merge/push/deploy需求权限。

回滚仅回滚平台代码和显式运行配置；保留已发布策略版本及历史事实。

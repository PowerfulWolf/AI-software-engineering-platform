# T044 C3 — 新基线校验与批准后的 Task 草稿

本阶段由 Astra 开发平台恢复流程，不是平台完成目录需求。

## 已形成的能力

- 原终态交付事实与新基线准备结果之间有了可调用的只读校验器。
- Host 与恢复校验复用相同公司知识/硬规则构建逻辑，规范漂移不能被旧批准掩盖。
- 已批准恢复可生成确定的新 Task 草稿，保留原 Task 关联与预算/约束，原产品批准/设计/计划不变。
- 新 Request 绑定单独保留在内存草稿中，不覆盖旧历史；没有写 Task、dispatch 或调用真实模型。

## 验证边界

临时 Git 和专用 MySQL，Agent/人工审批均为离线替身。覆盖同/新基线、源记录/策略匹配、
目标脏文件/未跟踪文件、公司知识变化、损坏 profile、批准缺失、确定重放及字节不变。
模型调用0；测试替身不是实际需求的人工批准、QA 或 Review。

全量870 passed /140.55s；Ruff/format493files、Mypy274files、offline lock/build、diff-check通过。
实际旧基线只读校验通过，原BLOCKED/revision3、15文件/capture955b3247及父审批关联保持一致。
没有保存真实恢复计划、创建真实Task草稿、写业务数据库或启动模型。

## 遗留与回滚

T044 保持进行中。下一步封存草稿/继承记录、重新分配资源与持久化新执行，接人类恢复入口、
seed receipt/provider admission；最后才运行真实 Coder→QA→Reviewer。
回滚为撤销本源码提交，无数据库或旧交付记录迁移。

# T048 continuation implementation record

Continued the existing `/private/tmp/ase-t047-t049-20260918` worktree and preserved the earlier
retrieval, gap, Learning, Web management and workflow implementation.

Corrections from independent QA/Review:

- Native guards preserve typed knowledge gaps and their recoverable Requirement wait.
- Knowledge consultation becomes a budgeted, persisted Context section before Delivery requests;
  native rules retain identities; model call usage/provider/model and output digests persist.
- Immutable search operation replay repairs interrupted evidence publication.
- Completion gates read actual sealed artifact chains and bind acceptance, independent producers,
  candidate revision, exact consultation binding and parent/final Context identities.
- Stage gates use validated durable checkpoint/design/plan/resolution facts for start, architecture,
  planning, recovery and failure analysis, with exact required receipts.
- First resolved-gap recovery keeps the original candidate. Later candidates reference the first
  exact recovery instead of repeatedly rejecting approved Requirement facts on revision change.
- One-time delivery admission preserves exact pre-upgrade sealed legacy artifacts and their original
  Context, while all new artifacts require knowledge evidence. No old receipt or verdict is invented.
- Human knowledge resolutions enter standard Evaluation HumanActionEvent before role work, so ADR
  preserves manual intervention. Reusable Learning still needs separate publication approval.
- Nullable zero-denominator evaluation metrics round-trip through canonical wire serialization.
- Queue wait adapter verifies persisted gap/route, exact binding/claim and owner fence before native
  queue.wait. Existing request runtime is not migrated into a per-role queue Worker.

Independent QA is in `qa.md`; independent review and its rechecks are in `review.md`. Those records
are maintained by separate agents. Global results are recorded in the combined continuation report.

Existing data and rollback: see `docs/planning-knowledge-operations.md`. No production data changed;
legacy artifacts are admitted once by exact digest, approvals/frozen selections remain immutable,
and gap recovery uses the existing resume API. Roll back code/schema together after stopping new
runs/index writes; retain all external evidence, admissions, jobs and immutable sources.

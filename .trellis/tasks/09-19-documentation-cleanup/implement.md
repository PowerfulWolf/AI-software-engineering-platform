# Documentation cleanup outcome

## Changes

- Added docs category navigation and a current engineering task index.
- Corrected Console/CLI entry, first-run setup, default model description and C++ fixture coverage.
- Archived the original roadmap, T047–T049 delivery report and Console recovery incident history.
  Migration records retain source revision, original digest and destination digest.
- Consolidated projection/static renderer into architecture, and typed tools into prompt-protocol.
  Historical Task allowed_paths remain unchanged; live verification references point to their new archive.
- Marked the already merged Console fix completed at c280310. Preserved all 7 independent reports,
  8 red/green logs and 3 intermediate reproduction scripts with SHA-256 mapping.
- Added legacy_unknown metadata for 11 historical task directories with no prior task.json;
  retained their documents without inventing acceptance. Added stable IDs to 3 name-only records.
- Removed two tracked unused placeholders; ignored root artifacts as generated runtime output.
- Removed 27 regenerable cache directories (3,395,862 bytes) and 4 empty/placeholder directories
  from the main checkout. Environment, Git metadata, external worktrees and business data were retained.

## Verification

Run `python3 .trellis/tasks/09-19-documentation-cleanup/verify.py` from the repository root.
The output is in [verification.json](verification.json); the check source is [verify.py](verify.py).
It checks Markdown relative file/section targets, metadata and unique IDs, archive index coverage,
baseline and migrated snapshot hashes/body preservation, original evidence and reproduction hashes,
candidate app.js identity, plus git diff --check.

An independent read-only check by /root/console_review found no blocking issues in the same scoped
navigation/history/evidence/legacy-state checks. It did not change files or create a runtime verdict.
Runtime source, public Schema and tests are unchanged; no model calls or unrelated runtime suites
were run for this documentation-only cleanup.

## Existing data and rollback

No Requirement, TaskRepository, Operation, approval, queue, configuration or credential facts changed.
Only engineering metadata was reconciled. If this repository is a platform target, its changed native
spec path/hash is handled by the ordinary preparation/drift rules; existing approvals are not rewritten.

Revert this documentation commit to restore prior files. Cache directories can be regenerated by
Python/pytest. Original report contents and all older commits remain available.

# Documentation cleanup

用户批准范围：修正文档错误并建立分类索引 → 归档交付和事故记录 → 收尾任务状态与证据 → 合并重复说明 → 清理缓存和空目录。

## Acceptance

- 使用、运维、架构、协议、决策、历史有明确导航；修正 CLI、setup、默认模型和 C++ 矩阵错误。
- 原交付、事故和旧路线记录保持历史正文；迁移记录包含旧路径、基线和摘要。
- c280310 的 Console 工程任务完成，独立报告原样持久化；未知历史状态不虚构完成。
- 三份短说明合入对应参考章节，现行链接指向新位置；旧任务 allowlist 保留原意。
- 仅清理本仓库缓存、已确认空目录或占位，保留环境、外置数据、活跃和脏 worktree。

## Verification and rollback

检查 Markdown 文件和新章节链接、task.json、归档覆盖率、证据 SHA-256、历史正文保留及
git diff --check。无运行时代码变更，不运行真实模型。回滚此次文档提交；缓存由 Python/pytest
重新生成，生产数据不变。

# 实现记录

- 统一页面、Section、字段解释为可访问的信息按钮与原生 Popover；使用纯文本，支持键盘 Enter、Escape、点击外部和显式关闭。
- 模型路由字段固定标签行与控件行，信息按钮不再推挤相邻输入；复选框与 API Key 输入垂直居中。
- Agent 模型分配详情把主模型、备用模型身份与添加备用模型选择器统一到 76px + 10px 后的内容列；窄屏保持同一基线。
- 保留 MySQL 连接结果、保存反馈、重启状态、密钥占位符等操作事实的直接展示；未修改保存接口或配置数据。

# T019 Verification

验收覆盖稳定 assignment/lease、批量优先级排序、active/waiting/expired lease 容量、retry
available time、inactive/role/capability/capacity 拒绝、自审隔离，以及 ModelRouter 的 default、
risk、complexity、failure escalation、context capacity 与 policy mismatch。结果：13 passed；全仓
324 passed。

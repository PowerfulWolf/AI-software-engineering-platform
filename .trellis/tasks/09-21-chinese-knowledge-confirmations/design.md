# 设计

在知识咨询的模型指令中要求中文，并在持久化 Gap 前执行确定性的中文检查。若问题不含中文字符，使用平台中文兜底问题；不调用翻译模型，也不改写模型证据。其余平台固定描述直接改为中文。历史记录保持不可变，Console 继续兼容旧英文固定决策文案。

学习建议审批的 `rationale` 是 Console 生成的审计描述，直接替换为等义中文，不改变 action、target 或发布流程。

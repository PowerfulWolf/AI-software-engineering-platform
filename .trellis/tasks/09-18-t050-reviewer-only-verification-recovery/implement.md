# T050 Implementation Plan

1. 更新 recovery spec、typed model 和 JSON Schema；
2. 增加 reviewer-only 的红灯 contract tests；
3. 从 terminal event 提取并验证可复用 QA；
4. 调整 Runner、Admission 和 FileRecoveryStore completion/invocation guards；
5. 运行 focused tests、schema、Ruff、Mypy 和 diff check；
6. 记录现有需求的继续操作与回滚方式。
